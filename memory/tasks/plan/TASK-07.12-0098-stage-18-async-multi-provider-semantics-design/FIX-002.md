# FIX-002: Async multi-provider design gate disposition

Status: applied
Requirement: required if design recommendation is approved
Related Task: [TASK-07.12-0098](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Created: 2026-07-13

## Мета

Закрити canonical async multi open questions, перевести product requirement із
`design-first` у `design-approved` і зафіксувати phased eligibility без implementation
approval або створення implementation tasks.

## Target files

- `memory/domain/open-questions.md`
- `memory/product/requirements.md`
- `memory/product/roadmap.md`
- `memory/state.md`

## Exact proposed changes

### `memory/domain/open-questions.md`

Замінити весь section `## Stage 18: async multi-providers` і наступний blocking paragraph
на:

```markdown
Немає.
```

### `memory/product/requirements.md`

Замінити row `REQ-ASYNC-MULTI-001` на:

```markdown
| REQ-ASYNC-MULTI-001 | Async multi-providers реалізуються phased зі sync-eligibility preflight для `getAll()`, окремим explicit async collection boundary без затвердженої design-ом назви, sequential registration-order resolution, no-partial-results, per-provider cache/retry/ownership, runtime/scope resource ownership і declarative inspection; implementation потребує окремих human-approved tasks. | design-approved |
```

Після пояснення `design-first` додати:

```markdown

`design-approved` означає, що human-reviewed semantic model може бути основою окремого
implementation planning; це не означає implemented API або автоматичне схвалення tasks.
```

### `memory/product/roadmap.md`

У Stage 18 після lifetime design gate paragraph додати:

```markdown
- Async multi design gate `TASK-07.12-0098`: semantic design approved; phased
  implementation eligible лише через окремі human-approved tasks у порядку contribution
  identity/preflight, container async collection resolution, composer/inspection,
  resource ownership, testing/DSL/docs. Design approval не затверджує public accessor
  name або implementation.
```

### `memory/state.md`

Після paragraph про `TASK-07.12-0097` додати:

```markdown
`TASK-07.12-0098` завершила async multi-provider semantics design gate: sync preflight,
sequential registration-order resolution, per-provider cache/retry/ownership,
no-partial-results і phased resource model design-approved. Runtime/public API ще не
реалізовано; exact async collection accessor name та implementation tasks потребують
окремого human decision.
```

У `## Наступні кроки` замінити items 1-2 на:

```markdown
1. Graph export tasks `TASK-07.12-0094`..`0096` завершені після human review; наступний
   graph-export work створювати лише за окремим scope.
2. Lifetime й async multi design-approved semantics переводити в bounded phased
   implementation planning лише за окремим human decision; async multi resource slice має
   йти після async factory/container/composer foundation.
```

## Rationale

Approved design дає decision-ready відповіді на всі чотири canonical questions, але не
повинен непомітно стати implementation approval.

## Impact and risks

- `design-approved` фіксує semantics, не API availability.
- Sequential latency і cleanup secondary-error visibility лишаються residual risks.
- Exact names, overloads, task count і release inclusion потребують наступного contract.

## Consistency check

- Domain/product/roadmap/state: included.
- Technical design/trace/testing: FIX-001.
- Indexes: not-needed.
- Historical sources: unchanged.

## Approval

Decision: approved by user on 2026-07-13
Applied: 2026-07-13; exact proposal verified
