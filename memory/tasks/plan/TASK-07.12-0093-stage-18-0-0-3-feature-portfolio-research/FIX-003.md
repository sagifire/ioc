# FIX-003: Stage 18 unresolved design gates

Status: applied
Requirement: required if design-first candidates remain in roadmap
Related Task: [TASK-07.12-0093](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Created: 2026-07-12

## Мета

Зберегти unresolved async multi-provider і lifetime-validation decisions як explicit
canonical open questions, а не приховані implementation assumptions.

## Target file

- `memory/domain/open-questions.md`

## Exact proposed changes

Замінити `Немає.` на:

```markdown
## Stage 18: lifetime dependency validation

- Яка explicit object-configuration declaration описує direct instance dependency та
  deferred factory/handle dependency без decorators, source parsing або factory execution?
- Яка severity matrix застосовується до singleton → scoped, singleton → ordinary
  transient, singleton → managed resource і child-scope capture?
- Як inspection/diagnostics ідентифікують private providers без витоку private token IDs?
- Який coverage contract чесно відрізняє proven unsafe capture від undeclared/unknown edge?

## Stage 18: async multi-providers

- Чи може sync `getAll()` читати collection, усі async contributions якого eager-ready
  після `freeze()`, чи наявність async contributor завжди вимагає explicit async API?
- Чи входять async resources у перший slice, і хто володіє успішними resources після
  partial collection failure?
- Які concurrency, fail-fast/aggregate error, retry і rollback semantics застосовуються
  per contribution?
- Як mixed sync/async contributions, eager/lazy state, scope-local values і deterministic
  registration order відображаються в inspection та testing overrides?

Ці питання блокують implementation відповідних candidates, але не блокують окремий
bounded graph-export slice.
```

## Rationale

Без canonical questions наступна implementation task може випадково зафіксувати
несумісні semantics.

## Impact and risks

- Питання не створюють accepted API або task backlog.
- Вони видаляються/закриваються лише через human-reviewed design decisions.

## Consistency check

- Domain open questions: included.
- Domain index: not-needed, file structure не змінюється.
- Product/technical context: `FIX-001` і `FIX-002`.

## Approval

Decision: approved by user on 2026-07-12
Applied: 2026-07-12; exact proposal verified
