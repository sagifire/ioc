# Architecture

Status: skeleton.

The detailed architecture documentation will be written after the relevant runtime layers
are implemented. The current implementation includes the core token API and Stage 4 sync
single-provider container API plus Stage 5 multi-provider contributions; scopes, composer,
DSL, diagnostics and adapters remain planned for later roadmap stages.
