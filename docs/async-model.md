# Async Model

Status: skeleton.

Stage 7 implements async single-provider bindings, explicit `getAsync()` resolution, eager
singleton initialization, lazy async initialization, async resources and runtime/scope
disposal. Detailed async model documentation is planned for the later documentation stage.
